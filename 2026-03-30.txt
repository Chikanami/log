29号（周日）
休息。
30号（周一）
完成了对映射的编码工作以及长期的后续模型方案审查，将相关只读数据和映射表塞进了数据库。因为重命名了数据库文件导致连接失效，重做了metabase对数据库的连接。
关于映射表和只读数据表的关系如下，虽然还是有点乱但是基本能理解到意思，反正是给自己看的，这样就有干净可用的维度表了

```mermaid              
    erDiagram              
              
    客户列表{              
        string  表备注        "第三方系统导出只读数据"              
        string  客户id  UK              
        string  所属区域      "每个区域节点使用斜杠分隔，与桥接映射表的Path字段属性相同"              
    }              
              
    map_erppath_to_region_id{               
        int  id  FK              
        string Path PK              
    }              
              
    map_region_id_to_hierarchy{              
        string  ID  UK              
        string  Lv1              
        string  Lv2              
        string  Lv3              
        string  Lv4              
        string  Lv5              
        string  Lv6              
        int Depth              
    }              
              
              
    客户列表}|--||map_erppath_to_region_id:""              
    map_region_id_to_hierarchy||--|{map_erppath_to_region_id:""              
```

前阵子研究了一下需求后发现，需要进行分析的对象至少需要五维甚至以上的多维矩阵才能够完整描述，而且很难在满足业务端需求的前提下降维
抛开时间和层级这两个基本维度后，作为研究对象的“客户”和”产品“分别需要[订单,销售金额,下单周期,产品结构,回款周期,新老,退货率,业务员拜访周期,所属渠道]、[受众结构,新老,订单,销售金额,品类,品牌,生产商,推广程度,退货率,库存,渠道结构]这些基本属性数据，而其中"客户.产品结构"和"产品.受众结构"是一个至少三维或四维的矩阵，这俩还是相互交叉的，"产品.渠道结构"至少是一个二维矩阵。没数错的话这辆整个对象应该是一个五维或者六维的高阶张量。
我的打算是把两个“画像矩阵”都拆出来做聚类（人工版）打标签降维成标量（因为是人工版这个活得拉着销售一块儿去干）后再塞回去，这样就能简单很多。处理“产品.渠道结构”矩阵暂时还没有思路，需要先去补一补线代，当年在学校里没怎么听课，不然很多事都干不了。   不过这都是起码个把俩月甚至好几个月之后的事了，暂时有个想法就行，该学的照样学，需要用的时候再学就来不及了，反正是早晚要用的东西。

计划这两天在metabase里多join几下用历史数据先把样板上线，之后用dbeaver连接两个.db，一个储存只读数据表和映射表还有其他之后可能会用到的中间表、桥接表、翻译表之类的东西，另一个.db储存抽取转换清洗之后的数据直接传输给metabase——这个估摸着也是起码个把俩月之后的事。

就是最开始几天动作慢了点，评估了一下那阵子的工作量和难度之后确信的确慢了点。
